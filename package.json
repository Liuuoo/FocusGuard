{
  "name": "focusguard",
  "version": "0.1.0",
  "private": true,
  "description": "Local usage monitor with password-protected settings.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/app-classifier.test.js tests/schedule.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
