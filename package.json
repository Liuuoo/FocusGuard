{
  "name": "focusguard",
  "version": "0.1.0",
  "private": true,
  "description": "Local usage monitor with password-protected settings.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
